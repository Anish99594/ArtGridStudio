{
  "name": "pinata-server",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.7.7",
    "pinata": "^1.10.1"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
